# bootstrap.yml 可以理解成系统级别的一些参数配置, 优先加载, nacos必须放在这里才可加载到
# application.yml 可以用来定义应用级别
spring:
  cloud:
    nacos:
      username: nacos
      password: nacos
      server-addr: 115.159.66.110:8848

# nacos配置禁用打印心跳日志
logging:
  level:
    com.alibaba.nacos.client.*: WARN